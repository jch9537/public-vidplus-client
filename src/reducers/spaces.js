import {
  ADD_SPACES,
  ADD_SPACE,
  EDIT_SPACE,
  DELETE_SPACE,
  SELECT_SPACE
} from "../actions/types";

import {
  addSpaces,
  addSpace,
  editSpace,
  deleteSpace,
  selectSpace
} from "../actions/creators";

export default function spaces(state = [], action) {
  return state;
}
